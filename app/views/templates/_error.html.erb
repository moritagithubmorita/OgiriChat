<div>
  <% if model.errors.any? %>
    <h2><%= pluralize(model.errors.count, "error") %>ありました。修正してください。</h2>
    <p>
      <% model.errors.full_messages.each do |message| %>
        <%= message %>
      <% end %>
    <% end %>
  </p>
</div>